train:
  num_samples: 1024
  n_epochs: 100
  batch_size: 1
  accumulation_steps: 128
  sample_rate: 24000
  learning_rate: 1e-4
  bos_token: 0
  weight_decay: 1e-6
  resume_from_checkpoint: "/home/infres/lasri-22/tts/outputs/2025-11-10/14-13-02/runs/tts_20251110_141303/checkpoints/model_final.pt"  # null or path to checkpoint

model:
  text_embed_dim: 512
  text_num_layers: 2
  num_decoder_layers: 2

rvq:
  n_bins: 1024
  n_quantizers: 32

dataset:
  root_dir: "./datasets/cmu_us_awb_arctic"
  wav_dir: "wav"
  text_file: "etc/txt.done.data"

logging:
  log_dir: "runs"
  save_every: 5000
